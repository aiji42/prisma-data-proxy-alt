{
  "name": "vercel",
  "version": "1.0.0",
  "main": "api/index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "rimraf dist && tsc index.ts --esModuleInterop --outDir dist",
    "deploy": "yarn build && vercel --prod"
  },
  "dependencies": {
    "@prisma/client": "^3.15.2",
    "apollo-server-express": "^3.9.0",
    "express": "^4.18.1",
    "prisma": "^3.15.2",
    "prisma-data-proxy-alt": "^1.1.1"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.7.3",
    "vercel": "^25.1.0"
  }
}
